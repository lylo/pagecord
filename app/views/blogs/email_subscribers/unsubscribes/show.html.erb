<%= render "blogs/header", hide_email_form: true %>

<div class="confirmation-card mt-12 text-center max-w-[450px] mx-auto p-8">
  <% if @subscriber.destroyed? %>
    <div class="text-body">
      You're now unsubscribed from <span class="font-bold"><%= @blog.display_name %></span> 👋
    </div>
  <% else %>
    <div class="text-body">
      To unsubscribe from <span class="font-bold"><%= @blog.display_name %></span>, click the link below.
    </div>
    <div class="mt-4">
      <%= button_to "Unsubscribe", email_subscriber_unsubscribe_path(@subscriber),
          class: "text-meta font-medium underline",
          data: { turbo: false } %>
    </div>
  <% end %>
</div>