<header>
  <%= render "blogs/social_links" %>

  <%= render "blogs/blog_titlebar" %>

  <% if @blog.bio.present? && !local_assigns[:hide_bio] %>
  <div class="bio">
    <%= auto_link @blog.bio %>
  </div>
  <% end %>

  <%= render "blogs/email_subscriber_form" unless local_assigns[:hide_email_form] %>
</header>