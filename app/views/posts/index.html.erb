<div class="mx-auto">
  <% @posts.each do |post| %>
    <%= render post %>
  <% end %>

  <% if @pagy.next %>
    <%== pagy_next_html(@pagy, text: "Older", link_extra: 'id="next_link"') %>
  <% end %>
</div>