<p>
  <%= t("comments.mailer.intro", count: @comments.size) %>
</p>

<% @comments.each do |comment| %>
  <div style="background-color: #F3F4F6; padding: 15px; border-radius: 5px; margin: 20px 0;">
    <p>
      <strong><%= comment.name %></strong> on <%= comment.post.display_title %>
    </p>

    <%# The blog renders this escaped with white-space: pre-line, but email
        clients can't be trusted with that, so paragraphs are built here.
        tags: [] because none of the commenter's markup should survive. %>
    <div>
      <%= simple_format(comment.message, {}, sanitize_options: { tags: [] }) %>
    </div>
  </div>
<% end %>

<p>
  <%= link_to t("comments.mailer.button"), app_comments_url %>
</p>
