<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Pagecord :: Admin</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= render "layouts/favicons" %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="flex flex-col min-h-screen bg-slate-50 dark:bg-slate-900">
    <%= render "admin/shared/header" %>
    
    <main class="container mx-auto px-5 flex flex-grow mb-8">
      <div class="mx-auto max-w-6xl w-full text-slate-950 dark:text-slate-50">
        <%= render "app/shared/flash" %>

        <%= yield %>
      </div>
    </main>
  </body>
</html>
