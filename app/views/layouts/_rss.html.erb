<% if @user&.persisted? %>
  <%= tag :link, rel: "alternate", type: "application/rss+xml", title: "Posts from @#{@user.username}", href: user_posts_path(username: @user.username, format: "rss") %>
<% end %>
