<% if @user&.persisted? %>
  <%= tag :link, rel: "alternate", type: "application/rss+xml", title: "Posts from @#{@user.username}", href: blog_posts_path(username: @user.username, format: "rss") %>
<% end %>
