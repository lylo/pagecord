<% pending_count = @blog.comments.pending.count %>
<% if pending_count.positive? %>
  <span class="ms-1.5 inline-flex items-center justify-center min-w-5 h-5 px-1.5 rounded-full bg-pagecord-accent text-white dark:text-slate-950 text-[11px] font-bold"><%= pending_count %></span>
<% end %>
