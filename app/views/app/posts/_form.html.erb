<%= form_with(model: [:app, @post], class: "mt-8") do |form| %>
  <%= form.text_field :title, placeholder: "Optional post title", class: "w-full placeholder-slate-300 dark:placeholder-slate-600 dark:bg-slate-800 border border-slate-300 dark:border-slate-700 text-slate-800 dark:text-slate-200 rounded-lg" %>

  <%= form.rich_text_area :content, placeholder: "Write your post...", class: "trix-content", data: { controller: "autogrow" }%>

  <%= form.submit class: "mt-4 btn-primary" %>
  <%= link_to "Cancel", app_posts_path, class: "btn-secondary" %>
<% end %>