@<%= @access_request.user.blog.subdomain %>

We received your login request. Click the link below to access your Pagecord account.

<%= access_request_verification_url(@access_request.token_digest) %>
