<% content_for :title, "Pagecord - Log In" %>

<div class="mb-8">
  <h1 class="text-2xl font-bold mb-2">Welcome back!</h1>
  <p class="text-slate-600 dark:text-slate-400">
    <%= @password_mode ? "Enter your subdomain and password to log in." : "Enter your subdomain and email. We'll send you a magic link to log in – no password required!" %>
  </p>
</div>

<%= turbo_frame_tag "login_form" do %>
  <%= render @password_mode ? "password_login_form" : "email_login_form" %>
<% end %>
